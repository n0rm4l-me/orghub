/**
 * A `notFound()` from an article or page renders here rather than at the root, so
 * the header survives and the reader has somewhere to go besides the one link in
 * the panel. Genuinely unmatched URLs still fall through to the root boundary.
 */
export { default } from "../not-found"
